---
all:
  children:
    control_plane:
      hosts:
        cp1:
          ansible_host:
    workers:
      hosts:
        worker1:
          ansible_host:
        worker2:
          ansible_host:
    k8s_cluster:
      children:
        control_plane:
        workers:
